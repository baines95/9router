{
  "name": "8router-tests",
  "version": "0.4.6-mini.2",
  "private": true,
  "type": "module",
  "description": "Unit tests for 8router embeddings endpoint",
  "scripts": {
    "test": "NODE_PATH=/tmp/node_modules /tmp/node_modules/.bin/vitest run --reporter=verbose",
    "test:watch": "NODE_PATH=/tmp/node_modules /tmp/node_modules/.bin/vitest --reporter=verbose"
  },
  "devDependencies": {
    "vitest": "^4.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}
